# GitHub 配置

- `workflows`：V2 CI、测试和镜像构建；当前尚未初始化。
- `ISSUE_TEMPLATE`：V2 Issue 模板；当前尚未初始化。
- `copilot-instructions.md`：引导 Copilot 读取分层 AGENTS 指令。

V1 工作流已归档到 `legacy/v1/.github/workflows`，不会在根目录继续生效。

