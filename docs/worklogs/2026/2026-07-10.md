# 2026-07-10 开发记录

## 今日目标

- 确定 V2 重构技术路线和产品范围。
- 生成完整重构方案。
- 开始 V1 归档与 V2 目录规范化。

## 已完成

- 确定 Vben、Ant Design Vue、FastAPI、PostgreSQL、Redis、ArtPlayer 和 hls.js 技术路线。
- 确定邀请制、多用户、用户提交源后管理员审核、豆瓣与 TMDB 双 Provider。
- 创建 `docs/PROJECT_REFACTORING.md`。
- 将 V1 运行代码和旧部署配置归档到 `legacy/v1`。
- 建立 V2 标准目录与分层 AI 指令。
- 完成 V1 静态系统审查，形成 8 份审查报告和功能去向矩阵。
- 修正实施顺序：在工程初始化前增加产品定义、流程原型和技术设计阶段。

## 重要决策

- 当前没有线上环境，不维护双版本运行入口。
- V1 作为只读历史快照保留。
- 每个自然日只维护一份开发汇总。
- Figma 为原型事实源，确认版本导出 PNG/PDF 入库。

## 修改范围

- 根目录结构。
- `legacy/v1` 归档。
- `docs`、`apps`、`packages`、`services`、`infra`、`scripts`、`tests` 骨架。
- 根、前端和后端 AI 指令。
- `docs/audits` V1 功能、架构、数据、播放、部署、安全和技术债报告。

## 验证结果

- V1 关键入口、播放器资源、三套 Serverless 代理和旧工作流均存在于 `legacy/v1`。
- V1 归档共包含 64 个文件。
- 根目录已不存在旧 HTML、Express、前端脚本和代理入口。
- 根目录旧 GitHub Actions 已失效并保存在归档目录。
- `legacy/v1/server.mjs` 已通过 Node.js 语法检查。
- 新版目录、说明文件及根/前端/后端三级 AI 指令均已创建。
- 15 个 V1 第一方 JavaScript 文件全部通过语法检查。
- V1 首页和播放器静态 HTML 均未发现重复 ID。
- 已确认默认选择的四个资源 key 均不存在于当前源配置。
- 开发机已具备 Git 2.54、Node.js 22.22、npm 11.17、Corepack、pnpm 11.9、Docker CLI 29.5 和 Docker Compose 5.1。
- 当前未检测到可用的 Python 与 uv。
- Docker CLI 已安装，但 Docker Engine 当前未运行或当前会话无法连接。

## 遇到的问题

- Figma 插件安装等待时间较长，暂缓处理，不阻塞目录规范化。

## 风险与待确认事项

- Vben 上游版本尚未正式拉取和锁定。
- Python 与 uv 尚未安装；Docker CLI 已存在但 Engine 当前未运行。
- TMDB API Key 尚未准备。
- 当前审查为静态结论，第三方 API、HLS 和各部署平台行为仍需隔离运行验证。

## 下一步

- 评审 V1→V2 功能矩阵中的待确认项。
- 编写 V2 产品需求、角色权限、信息架构和业务状态机。
- 绘制核心用户流程并生成低保真原型。
- 产品和原型确认后再准备开发环境并初始化工程。

## 相关链接

- [完整重构方案](../../PROJECT_REFACTORING.md)
- [目录结构说明](../../architecture/directory-structure.md)
- [V1 系统审查索引](../../audits/README.md)
